import Joi from "joi";

export const categorySubmissionValidator = {
  create: Joi.object({
    // no-data
  }),
  update: Joi.object({
    // no-data
  }),
};

export default categorySubmissionValidator;
